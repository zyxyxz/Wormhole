.empty { padding: 80rpx 0; display: flex; flex-direction: column; align-items: center; }
.empty-img { width: 160rpx; height: 160rpx; opacity: 0.4; }
.empty-text { margin-top: 20rpx; color: #9AA0A6; }


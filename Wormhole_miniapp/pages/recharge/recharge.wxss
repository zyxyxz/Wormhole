.container { padding: 24rpx; background: var(--bg); color: var(--text); min-height: 100vh; }
.label { margin-bottom: 16rpx; color: var(--text-muted); }
.amount { height: 80rpx; background: var(--input); border-radius: 8rpx; padding: 0 20rpx; margin-bottom: 20rpx; color: var(--text); }
.submit { background: var(--primary); color: #fff; }

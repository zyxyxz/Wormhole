page { overflow: hidden; }
.chat-container {
  display: flex;
  flex-direction: column;
  min-height: calc(100vh - 44px - constant(safe-area-inset-top));
  min-height: calc(100vh - 44px - env(safe-area-inset-top));
  height: calc(100vh - 44px - constant(safe-area-inset-top));
  height: calc(100vh - 44px - env(safe-area-inset-top));
}
/* 保证消息只在内部滚动，输入区固定在底部 */
.message-list { flex: 1; padding: 20rpx; height: calc(100vh - 120rpx); box-sizing: border-box; }
.message-item { display: flex; margin-bottom: 24rpx; }
.message-item.self { flex-direction: row-reverse; }
.avatar {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  margin: 0 16rpx;
  background: linear-gradient(135deg, #5EEAD4, #22D3EE);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  color: #0F172A;
  overflow: hidden;
}
.avatar image { width: 100%; height: 100%; object-fit: cover; }
.message-content { max-width: 70%; }
.nickname { font-size: 24rpx; color: #64748B; margin-bottom: 6rpx; }
.message-item.self .nickname { text-align: right; }
.message-bubble { background: transparent; padding: 0; border-radius: 0; word-break: break-word; border: none; color: #0F172A; box-shadow: none; }
.self .message-bubble { background: transparent; color: #0F172A; border: none; box-shadow: none; }
.message-time { font-size: 22rpx; color: #94A3B8; margin-top: 6rpx; }
.input-area { position: fixed; left: 0; right: 0; bottom: 0; padding: 16rpx; background: #FFFFFF; border-top: 1rpx solid #E2E8F0; display: flex; }
.message-input { flex: 1; height: 72rpx; background: #FFFFFF; border: 1rpx solid #E2E8F0; border-radius: 12rpx; padding: 0 24rpx; margin-right: 12rpx; color: #0F172A; }
.send-button { width: 120rpx; height: 72rpx; line-height: 72rpx; background: linear-gradient(135deg, #5EEAD4, #22D3EE); color: #0F172A; border-radius: 12rpx; font-size: 28rpx; } 

/**index.wxss**/
page {
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: #F8FAFC;
  color: #0F172A;
  font-family: "ZCOOL XiaoWei", "Songti SC", "STSong", "PingFang SC", serif;
}

.scrollarea { flex: 1; }

.aurora-bg {
  position: fixed;
  inset: 0;
  overflow: hidden;
  z-index: 0;
  background: radial-gradient(1200rpx 600rpx at 50% -20%, rgba(56,189,248,0.18), transparent 70%),
              linear-gradient(180deg, #F8FAFC 0%, #EEF2FF 55%, #F8FAFC 100%);
}

.blob {
  position: absolute;
  border-radius: 50%;
  filter: blur(0);
  opacity: 0.35;
  animation: float 10s ease-in-out infinite;
}
.blob-a { width: 320rpx; height: 320rpx; background: radial-gradient(circle at 30% 30%, #38BDF8, transparent 60%); top: -80rpx; left: -40rpx; }
.blob-b { width: 420rpx; height: 420rpx; background: radial-gradient(circle at 30% 30%, #60A5FA, transparent 65%); top: 180rpx; right: -160rpx; animation-delay: -2s; }
.blob-c { width: 300rpx; height: 300rpx; background: radial-gradient(circle at 30% 30%, #A7F3D0, transparent 60%); bottom: -120rpx; left: 120rpx; animation-delay: -4s; }

.grid-glow {
  position: absolute;
  inset: 0;
  background-image: linear-gradient(rgba(148,163,184,0.12) 1px, transparent 1px),
                    linear-gradient(90deg, rgba(148,163,184,0.12) 1px, transparent 1px);
  background-size: 60rpx 60rpx;
  mask-image: radial-gradient(circle at 50% 30%, black 10%, transparent 70%);
  opacity: 0.2;
}

.container {
  position: relative;
  z-index: 1;
  min-height: 100vh;
  padding: 40rpx 40rpx calc(50rpx + env(safe-area-inset-bottom));
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 10rpx;
}

.brand {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.brand-logo {
  width: 72rpx;
  height: 72rpx;
  border-radius: 20rpx;
  background: linear-gradient(135deg, #60A5FA, #34D399);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 700;
  color: #0F172A;
  letter-spacing: 2rpx;
  box-shadow: 0 16rpx 40rpx rgba(15,23,42,0.12);
}

.brand-text { display: flex; flex-direction: column; }
.brand-title { font-size: 34rpx; font-weight: 700; color: #0F172A; }
.brand-subtitle { font-size: 20rpx; color: rgba(15,23,42,0.6); letter-spacing: 1rpx; }

.join-pill {
  padding: 12rpx 22rpx;
  border-radius: 999rpx;
  border: 1rpx solid rgba(148,163,184,0.45);
  color: #0F172A;
  font-size: 24rpx;
  background: rgba(255,255,255,0.7);
  backdrop-filter: blur(6rpx);
}

.bottom-stack {
  margin-top: auto;
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.enter-card {
  padding: 28rpx;
  border-radius: 24rpx;
  background: rgba(255,255,255,0.9);
  border: 1rpx solid rgba(148,163,184,0.35);
  box-shadow: 0 20rpx 50rpx rgba(15,23,42,0.12);
}

.input-display {
  display: flex;
  justify-content: space-between;
  gap: 16rpx;
  margin-bottom: 6rpx;
}

.code-slot {
  flex: 1;
  height: 88rpx;
  border-radius: 16rpx;
  background: #F8FAFC;
  border: 1rpx solid rgba(148,163,184,0.35);
  display: flex;
  align-items: center;
  justify-content: center;
}

.code-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: #0F172A;
  box-shadow: 0 0 14rpx rgba(15,23,42,0.2);
}

.keypad-wrap { padding-bottom: 10rpx; }

.keypad {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18rpx;
}

.key-button {
  height: 102rpx;
  border-radius: 20rpx;
  background: rgba(255,255,255,0.95);
  border: 1rpx solid rgba(148,163,184,0.35);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 12rpx 26rpx rgba(15,23,42,0.08);
}

.key-button:active { transform: scale(0.98); }

.key-text {
  font-size: 34rpx;
  color: #0F172A;
  font-family: "SF Pro Rounded", "DIN Alternate", "PingFang SC", sans-serif;
}

.key-delete .key-text { font-size: 26rpx; color: rgba(15,23,42,0.6); }

.key-empty { background: transparent; border-color: transparent; box-shadow: none; }

@keyframes float {
  0% { transform: translateY(0); }
  50% { transform: translateY(-20rpx); }
  100% { transform: translateY(0); }
}
